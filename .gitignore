.venv
.vscode
.pdm-python
__pycache__
.ruff_cache
.mypy_cache
data/raw/
*.parquet
data/processed/*.csv
data/processed/*.parq
# Showcase data is course material, not working output: it ships with the repo so a
# lesson runs without a download.
!data/showcase/*.parquet
# The IRC corpus is the exception at 24 MB. It lives on the hub as
# pttrn-io/ubuntu-irc-days, and wa_analyzer.data fetches it on first use.
data/showcase/ubuntu_irc_days.parquet
*.log
*.pptx
.DS_Store
*.egg-info
# Build output. A committed sdist is how 32 MB got into this repository's history.
dist/
build/
*reference.json
.lycheecache
.notebookcache
**/personal.ipynb
config.toml
.aider*
data/processed/**
dev/
.current_values.json
# Per-student assistant memory and local hook overrides; never part of the course material.
CLAUDE.local.md
AGENTS.local.md
.cursor/
.lefthook-local.yml
lefthook-local.yml
