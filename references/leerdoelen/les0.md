De student:

0.1 kan een LLM met MCP gebruiken als gesprekspartner tijdens het werk, en kan elke eigen stap verdedigen zonder dat de assistent erbij is
0.2 begrijpt wat een class is, wat `__init__` en andere dunder-methodes doen, wat `@dataclass` je bespaart, en wat overerving (`class Child(Parent)`) oplevert — de basis waar `TransformBase` (les 1) en `BasePlot` (les 2) op voortbouwen
0.3 begrijpt wat een pad is (absoluut, relatief, `pathlib.Path`), waar een script "staat" als het draait, en wat de PATH-variabele van het systeem doet